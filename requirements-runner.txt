# ChallengeCtl Runner Dependencies

# HTTP client
requests>=2.31.0
websocket-client>=1.9.0
python-socketio>=5.10.0

# Configuration
PyYAML>=6.0
# All existing challengectl dependencies for SDR operations
# (Assumes GNU Radio and gr-osmosdr already installed)
