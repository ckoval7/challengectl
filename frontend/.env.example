# Frontend Configuration
# Copy this file to .env.local and customize for your environment

# Note: Authentication uses httpOnly cookies for session tokens (not API keys)
# API keys are only used for runner authentication on the backend

# WebSocket server URL (defaults to current host if not set)
# VITE_WS_URL=http://localhost:8443
